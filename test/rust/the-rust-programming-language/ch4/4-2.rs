#![allow(unused_variables)]
fn main() {
    /*
     * Because integers are simple values with a know, fixed size,
     * and these two 5 values are pushed onto the stack. This Data
     * types are stored entirely on the stack.
     */
    let x = 5;
    let y = x;
}