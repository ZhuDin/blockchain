#![allow(unused_variables)]
fn main() {
    let mut s = String::from("lo");
    // The push method taks a single character as a parameter and adds it to the String
    s.push('l');
    println!("{}", s);
}